id: design-system
type: library
language: node
stack: frontend

project:
  name: design-system
  description: Design System
  owner: bigzoo
  maintainers: [enzo.givernaud]

tasks:
  dev:
    command: 'pnpm run watch'
    local: true
    options:
      runInCI: false
    inputs:
      - 'src/**/*'
    deps:
      - 'theme:dev'

  build:
    command: "pnpm run build"
    deps:
      - 'theme:build'